#include "monty.h"

/**
 * _nop - does nott do anything.
 * @stack: pointer of doubly linked list.
 * @line_number: number of line.
*/
void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
